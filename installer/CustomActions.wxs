<?xml version="1.0" encoding="utf-8"?>
<!--
  NOTE: This file is intentionally empty.

  The custom actions (RegisterPropertySchema, UnregisterPropertySchema,
  RestartExplorer) are defined in Product.wxs, which is what the documented
  build command compiles. This file previously contained a stale duplicate
  copy of those actions; building it together with Product.wxs would fail
  with duplicate-symbol errors.
-->
<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs">
</Wix>
